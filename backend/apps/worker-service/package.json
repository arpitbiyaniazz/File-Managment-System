{
  "name": "@file-manager/worker-service",
  "version": "1.0.0",
  "private": true,
  "description": "Background Worker Service (RabbitMQ Consumers)",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint placeholder'"
  },
  "dependencies": {
    "@elastic/elasticsearch": "^8.12.0",
    "@file-manager/database": "workspace:*",
    "@file-manager/shared-types": "workspace:*",
    "@file-manager/shared-utils": "workspace:*",
    "amqplib": "^0.10.3",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.5",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
