{
  "name": "@file-manager/database",
  "version": "1.0.0",
  "private": true,
  "description": "Prisma database client and schema for the File Management System",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:migrate:prod": "prisma migrate deploy",
    "db:push": "prisma db push",
    "db:seed": "tsx prisma/seed.ts",
    "db:studio": "prisma studio",
    "db:reset": "prisma migrate reset",
    "build": "echo 'database package uses prisma generate'",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.9.0",
    "bcrypt": "^6.0.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "prisma": "^5.9.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
